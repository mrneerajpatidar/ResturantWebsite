import React from "react";
import { useSelector } from "react-redux";


const CartPage = () => {

const {} =useSelector((state) => state.allCart)

    return (
        <>
        <div>Cart Page</div>
        </>

    );
};

export default CartPage;